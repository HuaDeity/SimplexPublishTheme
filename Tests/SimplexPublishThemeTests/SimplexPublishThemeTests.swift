import XCTest
@testable import SimplexPublishTheme

final class SimplexPublishThemeTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SimplexPublishTheme().text, "Hello, World!")
    }
}
